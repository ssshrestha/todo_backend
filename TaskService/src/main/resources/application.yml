server:
  port: 8085
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/registerdb
  application:
    name: task-service
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
